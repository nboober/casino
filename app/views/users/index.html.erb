<%= link_to "Back to Admin Page", admin_path, :class => "btn btn-outline-dark" %>

<div class="center-page">

<h1>All Users</h1>

<% @users.each do |user| %>
  <%= link_to "#{user.first_name} #{user.last_name}", user_path(user) %><br><br>
<% end %>

</div>